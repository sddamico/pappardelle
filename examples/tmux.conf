# --- Pappardelle recommended settings ---

# Mouse support — click panes, drag to resize, scroll to browse history
set -g mouse on

# Focus events — enables dim-on-unfocus hooks (helps distinguish active pane)
set -g focus-events on

# Dim unfocused panes when the terminal loses focus. Scoped to the current
# window (-w) — setting these globally repaints every pane in every window on
# each focus transition, which reads as a full-screen flash
set-hook -g client-focus-out 'set -w window-style fg=colour245; set -w window-active-style fg=colour245'
set-hook -g client-focus-in 'set -uw window-style; set -uw window-active-style'

# Navigate between panes with Ctrl+Shift+arrow keys (no prefix needed)
bind -n C-S-Left select-pane -L
bind -n C-S-Right select-pane -R
bind -n C-S-Up select-pane -U
bind -n C-S-Down select-pane -D

# Cycle panes with PageUp/PageDown — handy for doom-coding on your phone
# with Termius, where Ctrl+arrow combos aren't available
bind -n PageUp select-pane -t :.-
bind -n PageDown select-pane -t :.+

# Mouse scroll — enter copy mode on scroll up, passthrough on scroll down
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" \
  "send-keys -M" "copy-mode -e; send-keys -M"
bind -n WheelDownPane send-keys -M

# Clean status bar — just show the session name
set -g status-style 'bg=colour235,fg=colour255'
set -g status-left-length 100
set -g status-left '#{?client_prefix,#[bg=colour208]#[fg=colour0] ^B ,#[bg=colour39]#[fg=colour0] #S }'
set -g window-status-format ''
set -g window-status-current-format ''
set -g status-right ''
